# recursive_reasoning
Combine iterative latent refinement from TRM with autoregressive text generation. The model refines continuous latent representations z through multiple iterations, then conditions standard AR decoding on the final latent state to generate tokens sequentially.
